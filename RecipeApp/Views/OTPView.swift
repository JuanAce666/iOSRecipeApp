//
//  OTPView.swift
//  RecipeApp
//
//  Created by Juan Felipe Acevedo Ramirez on 9/05/24.
//

import SwiftUI

/*struct OTPView: View {
    @Binding var otpText: String
    @Environment(\.dismiss) private var dismiss
    
    var body: some View {
        VStack(alignment: .leading, spacing: 15) {
            Button(action: {
                dismiss()
            }, label: {
                Image(systemName: "arrow.left")
                    .font(.title2)
                    .foregroundStyle(.gray)
            })
            .padding(.top, 20)
            Text("Enter OTP")
                .font(.largeTitle)
                .fontWeight(.heavy)
                .padding(.top, 10)
            
            Text("An 6 digits code has been sent to your Email ID")
                .font(.caption)
                .fontWeight(.semibold)
                .foregroundStyle(.gray)
                .padding(.top, -5)
            
            VStack(spacing: 25) {
                GradientButton(title: "Send Link", icon: "arrow.right") {

                }
                .hSpacing(.trailing)
                .disableWithOpacity(otpText.isEmpty)
            }
            .padding(.top, 20)
        }
        .padding(.vertical, 15)
        .padding(.horizontal, 25)
        .interactiveDismissDisabled()
    }
}

#Preview {
    CircleAnimation(isAuthenticated: .constant(false))
}*/
